query GetCourseById($id: UUID!) {
    courseById(id:$id) {
        id
        name
        subject
        instructor {
          id
          firstName
          lastName
          salary
        }
      }
}